package com.nba.sdui.core.config

/**
 * Library-wide defaults for the SDUI renderer.
 *
 * Image fallbacks: prefer `fallbackThumbnailUrl` / atomic `placeholder` from
 * the server; clients use neutral placeholder UI (initials, empty state) as a
 * last resort — not hardcoded third-party image URLs.
 */
object SduiDefaults {
    // Reserved for future shared non-image defaults (see kdoc above).
}
